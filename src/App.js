import { Box } from "@mui/material"
import React, { useContext } from "react"
import { AppRouter } from "./router"


export const  App = ()=>{
   return (
      <Box>
          <AppRouter/>
      </Box>
   )
}
